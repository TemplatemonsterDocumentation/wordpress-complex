<article><h2>Range</h2>

<p>The Range filter type allows setting the certain range within which one has to query the prices.</p>

<figure class="img-polaroid">
        <img src="projects/<?php echo $project;?>/img/creating/cr-6.png">
    </figure></li></p>

<ul class="marked-list">	

<p><li><b>Values prefix</b> - set the prefix to be shown before the values (e.g., the currency sign).</p></li>
<p><li><b>Values suffix</b> - set the suffix to be shown after the values  (e.g., the currency sign).</p></li>
<p><li><b>Thousands separator</b> - here you can define the separator for thousands values (e.g. space).</p></li>
<p><li><b>Decimal separator</b> - here you can define the separator for decimal values.</p></li>
<p><li><b>Number of decimals</b> - here you can specify the number of decimals after the separator.</p></li>
<p><li><b>Min Value</b> - here you can specify the minimum value available for filtering.</p></li>
<p><li><b>Max Value</b> - here you can specify the maximum value available for filtering. </p></li>
<p><li><b>Step</b> - here you can set the value of step applying to the filter.</p></li>
<p><li><b>Get min/max dynamically</b> - in case you’re working with prices, you can enable auto select for the minimum or maximum values. These values will be defined automatically depending on the actual min and max product prices.</p></li>
<p><li><b>Query Variable</b> - fill in the needed meta field slug (this meta field will become the source of the range values). E.g., you can specify the _price meta key to allow working with the Price range.</p></li></ul></article>